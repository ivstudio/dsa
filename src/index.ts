import { isAnagram } from './algorithms/anagram/anagram';

console.log(isAnagram('listen', 'silent')); // true
console.log(isAnagram('anagram', 'nagaram')); // true
console.log(isAnagram('rails safety', 'fairy tales')); // false
